import { PrivategptApi, Stream } from 'privategpt-sdk-utils';
declare type GetAssistantResponse<T extends any[], R> = (...args: T) => R;
export declare function getAssistantResponse<T extends any[], R extends Promise<Stream<any>>>({ fn, args, onNewMessage, abortController, }: {
    fn: GetAssistantResponse<T, R>;
    args: T;
    onNewMessage?: (completion: PrivategptApi.OpenAiCompletion) => void;
    abortController?: AbortController;
}): Promise<string>;
export {};
